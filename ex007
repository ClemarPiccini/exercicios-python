#desenvolva um programa que calcule as notas de um aluno e mostre sua media.
n1 = float(input('insira uma nota:'))
n2 = float(input('insira outra nota:'))
média = (n1 + n2)/2
print('a média das notas {} e {} é {}'.format(n1, n2, média))
